entry_points:
  main:
    parameters:
      train_path: {type: string, default: '/home/users/datasets/criteo/'}
      tol: {type: float, default: 0.001}
    command: "python train.py {train_path} {tol}"
